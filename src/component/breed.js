import React from 'react';

const breed = (props) => {
    
    return (
        <div>
            <img style={{ width: 300, height: 300 }} src={props.url}/>
        </div>
        )

}

export default breed;